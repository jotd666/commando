- strange crashes, memory corruption somewhere, confirmed on real HW,
  seen on HD (no whdload) mostly and floppy
  and on floppy on emulator: scrolling feed gets garbage and ends up locking the machine
  also path gets blocks for no reason
  
- add missing heli sound
- blade issue on the left when lands
- blits disappear too soon at the bottom of the screen
- add looped sounds (siren, heli) find loop stop command
- level skipping while at the boss shifts the levels: to test again

- manual optimization phase to remove PUSH/POP SR/MAKE_xx
- a6d1: optimize hl *= 32
- irq: optimize don't save regs
- remove code at 870C
- set DSW difficulty lives, start level!!
- cheat add 10000 to score
- highscore load/save

- when scrolling up, bridge sprites have some delay. 

rip sounds:

bpset 8717,,{A=5;g}

service mode: put 1 in E001 during intro

- rip sounds using service mode (https://vgmrips.net/packs/pack/commando-arcade)
